<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/favicon-8c.png">
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta name="Description" content="{{ renderData.description or description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
  </head>
  <body>
    <header class="main-header">
      <h1 class="home">
        <a href="{{ '/' | url }}">{{ metadata.title }}</a>
      </h1>

      {#- Read more about `eleventy-navigation` at https://github.com/11ty/eleventy-navigation #}
      {{ collections.all | eleventyNavigation | eleventyNavigationToHtml({
        listClass: "nav",
        listItemClass: "nav-item",
        activeListItemClass: "nav-item-active",
        activeKey: eleventyNavigation.key
      }) | safe }}
    </header>

    <main {% if templateClass %} class="{{ templateClass }}" {% endif %}>

      {{ content | safe }}
    </main>

    <footer class="main-footer">
      <p>Building Blocks for an Inclusive Web</p>
      <nav aria-label="External links">
        <ul>
          <li>
            <a href="https://ui.tournant.dev">Tournant UI</a>
          <li>
            <a href="https://github.com/tournantdev">GitHub</a>
          <li>
            <a href="https://twitter.com/tournantdev">Twitter</a>
        </ul>
      </nav>
      <p>This site is build with <a href="https://www.11ty.dev/">Eleventy</a> and hosted by <a href="https://www.netlify.com">Netlify</a>.</p>
    </footer>

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
